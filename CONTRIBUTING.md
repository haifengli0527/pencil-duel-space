# 貢獻指南

謝謝你願意花時間幫忙!這個專案刻意維持最簡單的形式,參與門檻也盡量低。

## 專案結構

整個遊戲就是**一個檔案**:`pencil.html`。裡面包含 HTML、CSS、JavaScript,沒有 npm、沒有建置流程、沒有相依套件安裝(唯一的外部資源是透過 CDN 載入的 Google Fonts 和 PeerJS)。

請盡量維持這個「單檔、無建置」的形式,除非有充分理由需要拆檔(例如專案複雜度大幅增加時,可以另外開 issue 討論)。

## 開發方式

1. Fork 這個 repo,clone 到本機。
2. 直接用瀏覽器打開 `pencil.html` 就能測試——修改後重新整理頁面即可看到結果,不需要任何指令。
3. 若要測試連線對戰功能,可以開兩個瀏覽器分頁(或分享給朋友),一邊建立房間、一邊輸入代碼加入。

## 程式風格

- 純 vanilla JavaScript(ES6+),不要引入框架或建置工具。
- 顏色、間距等視覺相關數值盡量用 CSS 變數(`:root` 裡的 `--xxx`),方便之後統一調整。
- 遊戲數值(棋子大小、力道、誤差角度、基地判定範圍等)集中寫在檔案開頭的常數區塊,並附上簡短註解說明用途。
- 涉及「手感」相關的改動(例如彈射誤差、基地判定範圍),請在 PR 描述裡說明你實際測試過的手感如何,方便 review。

## 回報問題 / 提出想法

歡迎開 issue 討論:

- Bug 回報:請附上重現步驟(本機雙人 / 連線對戰、用什麼瀏覽器)。
- 新功能或平衡性調整的想法:歡迎先開 issue 討論方向,再動手做 PR,避免白工。

## Pull Request

- 一個 PR 盡量只做一件事,方便 review。
- 修改遊戲手感或平衡性的 PR,請描述改動前後的差異與理由。
